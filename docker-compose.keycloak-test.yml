services:
  keycloak-db-test:
    image: postgres:15
    container_name: keycloak-db-test
    environment:
      POSTGRES_DB: keycloak
      POSTGRES_USER: keycloak
      POSTGRES_PASSWORD: keycloak
    volumes:
      - keycloak-db-test-data:/var/lib/postgresql/data
    networks:
      - keycloak-test-net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U keycloak"]
      interval: 10s
      timeout: 5s
      retries: 5

  keycloak-test:
    image: quay.io/keycloak/keycloak:22.0
    container_name: keycloak-test
    environment:
      KC_DB: postgres
      KC_DB_URL: jdbc:postgresql://keycloak-db-test:5432/keycloak
      KC_DB_USERNAME: keycloak
      KC_DB_PASSWORD: keycloak
      KC_HOSTNAME: localhost
      KC_HOSTNAME_PORT: 8081
      KC_HOSTNAME_STRICT: false
      KC_HOSTNAME_STRICT_HTTPS: false
      KC_HTTP_ENABLED: true
      KC_HEALTH_ENABLED: true
      KC_METRICS_ENABLED: true
      KEYCLOAK_ADMIN: admin
      KEYCLOAK_ADMIN_PASSWORD: admin
    ports:
      - "8081:8080"  # Map container port 8080 to host port 8081
    command:
      - start-dev
    depends_on:
      keycloak-db-test:
        condition: service_healthy
    networks:
      - keycloak-test-net
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health/ready"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s

networks:
  keycloak-test-net:
    driver: bridge

volumes:
  keycloak-db-test-data: 