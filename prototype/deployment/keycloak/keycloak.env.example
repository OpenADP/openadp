# Keycloak Environment Configuration
# Copy this to .env and update the passwords

# Keycloak URL (use localhost for local development, auth.openadp.org for production)
KEYCLOAK_URL=https://auth.openadp.org

# Keycloak admin password (change this!)
ADMIN_PASSWORD=your-secure-admin-password-here

# Optional: Customize Java heap for your Pi's RAM
# For Pi 4 with 4GB RAM, these defaults should work
# For Pi 4 with 8GB RAM, you could increase to -Xms1024m -Xmx2048m
# JAVA_OPTS_APPEND="-Xms512m -Xmx1024m" 